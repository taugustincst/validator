name: test
on: [push, pull_request]
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 22]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - run: npm test
      - run: node bin/ecw-validate.js validate --baseline examples/baseline.xlsx --actual examples/ecw-export.xlsx --catalog examples/catalog.xlsx --out report.xlsx --fail-on none
